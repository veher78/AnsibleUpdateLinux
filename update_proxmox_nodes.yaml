---
# =============================================================================
# Proxmox Hypervisor Patching + Rolling Reboot Playbook
# =============================================================================
# Purpose  : Patch all Proxmox nodes, detect kernel upgrades, migrate VMs/CTs,
#            reboot nodes one-by-one, restore non-migratable VMs after reboot.
# Author   : Generated for Ansible Semaphore
# Requires : community.general collection, root SSH access to Proxmox nodes
#
# Non-migratable VMs (powered off before reboot, restored after):
#   VM 101 → node 192.168.1.23
#   VM 103 → node 192.168.1.22
#   VM 110 → node 192.168.1.21
#   VM 111 → node 192.168.1.21
#
# CHANGELOG (this revision):
#   - Play 4 / step 4-C: VM and CT migration target selection is now LOAD-AWARE.
#     Instead of always picking the first conflict-free candidate in inventory
#     order (which caused VMs to ping-pong between the same two nodes across
#     a rolling reboot), we now query each conflict-free candidate's current
#     weighted load (vCPU + RAM, same metric as Play 6) and pick the lowest.
#   - Play 6: rebalancing is more aggressive — tighter balance_threshold and
#     a higher move cap, so the cluster converges closer to flat instead of
#     stopping at "close enough".
#   - Play 6: fixed qm migrate running from a fixed coordinator node instead
#     of the VM's actual current node (delegate_to: "{{ item.from }}"), and
#     added a per-run "moved once" guard so a VM can't be flip-flopped back
#     and forth when the cluster can't hit the threshold exactly.
#   - Play 4 & Play 6: RAM is now weighted 4x higher than CPU in the load
#     score (ram_weight: 4, cpu_weight: 1) — RAM balancing is the priority,
#     CPU is a lightweight tiebreaker only.
#   - Play 4 & Play 6: pinned/non-migratable VMs are now defined ONCE (the
#     anchored non_migratable_vms map in Play 4) and aliased into Play 6,
#     instead of Play 6 keeping its own separate, driftable copy. This fixes
#     a real bug where VM 119 (pinned to PVE1-dell) was missing from Play 6's
#     old hardcoded pinned list and got rebalanced away despite being pinned.
#   - Play 6: LXC containers are now included in rebalancing, not just QEMU
#     VMs. Previously the script explicitly filtered to type=="qemu", so
#     containers were invisible to both the load score and the move plan.
#     Anti-affinity parsing now also recognises "ct:" resource-ids in
#     rules.cfg (was "vm:" only), and the move executor picks the right
#     command per guest type (qm migrate --online for VMs, pct migrate
#     --restart for containers).
# =============================================================================

##############################################################################
# PLAY 1 – Gather pre-patch kernel version on every Proxmox node
##############################################################################
- name: "PLAY 1 | Record current kernel on all Proxmox nodes"
  hosts: proxmox_nodes
  gather_facts: false
  become: true

  tasks:
    - name: Get running kernel version
      ansible.builtin.command: uname -r
      register: kernel_before
      changed_when: false

    - name: Store kernel version as host fact
      ansible.builtin.set_fact:
        kernel_version_before: "{{ kernel_before.stdout }}"

    - name: "Show pre-patch kernel"
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} — kernel before patching: {{ kernel_version_before }}"


##############################################################################
# PLAY 2 – Patch every Proxmox node (apt upgrade)
##############################################################################
- name: "PLAY 2 | Apply all available updates on Proxmox nodes"
  hosts: proxmox_nodes
  gather_facts: false
  become: true
  serial: 1          # one node at a time for patching too (safer on small clusters)

  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 0

    - name: Upgrade all packages (full-upgrade)
      ansible.builtin.apt:
        upgrade: full
        autoremove: true
        autoclean: true
      register: apt_upgrade_result

    - name: "Show upgrade summary"
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} — packages changed: {{ apt_upgrade_result.changed }}"


##############################################################################
# PLAY 3 – Detect whether a new kernel was installed
#
# Package naming on modern Proxmox VE (8.x+):
#   proxmox-kernel-<ver>-pve-signed   ← the actual bootable image (status: ii)
#   proxmox-kernel-<ver>              ← meta/helper package (also ii, ignore)
#
# uname -r returns e.g.  7.0.2-6-pve
# dpkg version field is  7.0.2-6        (no trailing -pve)
#
# Strategy:
#   1. Query only 'ii' proxmox-kernel-*-pve-signed packages
#   2. Extract the version field (e.g. 7.0.2-6)
#   3. Append -pve to match uname -r output
#   4. Sort by version, take the highest
#   5. Compare against the running kernel recorded in Play 1
##############################################################################
- name: "PLAY 3 | Detect new kernel installation"
  hosts: proxmox_nodes
  gather_facts: false
  become: true

  tasks:
    - name: Find the highest installed proxmox kernel version
      ansible.builtin.shell: |
        dpkg -l 'proxmox-kernel-*-pve-signed' 2>/dev/null \
          | awk '/^ii/{print $3}' \
          | sort -V \
          | tail -1
      register: kernel_pkg_version
      changed_when: false

    # kernel_pkg_version.stdout is e.g. "7.0.2-6"
    # uname -r (kernel_version_before) is e.g. "7.0.2-6-pve"
    # Append -pve so the formats match for comparison.
    - name: Normalise installed kernel version to uname -r format
      ansible.builtin.set_fact:
        kernel_version_installed: "{{ kernel_pkg_version.stdout }}-pve"
      when: kernel_pkg_version.stdout != ''

    - name: Set new-kernel flag
      ansible.builtin.set_fact:
        new_kernel_installed: >-
          {{
            kernel_pkg_version.stdout != '' and
            kernel_version_installed != kernel_version_before
          }}

    - name: "Kernel upgrade status"
      ansible.builtin.debug:
        msg: >-
          {{ inventory_hostname }} —
          new kernel detected: {{ new_kernel_installed }}
          (running: {{ kernel_version_before }},
           latest installed: {{ kernel_version_installed | default('n/a') }})


##############################################################################
# PLAY 4 – Rolling reboot (only for nodes where a new kernel was installed)
#
# Strategy per node:
#   1. Power off non-migratable VMs pinned to this node
#   2. Migrate all migratable VMs/CTs to another online node (load-aware)
#   3. Reboot the node
#   4. Wait for node to come back and confirm cluster health
#   5. Restore non-migratable VMs
##############################################################################
- name: "PLAY 4 | Rolling reboot of Proxmox nodes with new kernel"
  hosts: proxmox_nodes
  gather_facts: false
  become: true
  serial: 1          # ← critical: process nodes strictly one at a time

  vars:
    # -----------------------------------------------------------------------
    # Non-migratable VM map:  node_ip  ->  list of VMIDs to power-off/restore
    # Key = ansible_host (IP) because that is how Ansible tracks the host at
    # runtime. These are used in 4-B and 4-G tasks below.
    #
    # Anchored (&non_migratable_vms_map) so Play 6 can alias the EXACT same
    # map instead of keeping its own separate, driftable copy. This is the
    # single source of truth for "VMs that must stay put" — a VM listed here
    # is excluded from both the Play 4 migration logic (it's simply never a
    # candidate, it gets shut down/restarted in place) and Play 6's rebalance
    # (see pinned_vmids there). Previously Play 6 had its own hardcoded
    # subset of these IDs that fell out of sync with this map — that's what
    # let VM 119 get rebalanced away from PVE1-dell despite being pinned here.
    # -----------------------------------------------------------------------
    non_migratable_vms: &non_migratable_vms_map
      "192.168.1.21": ["110", "111", "119"]
      "192.168.1.22": ["103", "120"]
      "192.168.1.23": ["101", "126"]

    # -----------------------------------------------------------------------
    # migration_candidates: ordered list of OTHER nodes to try for migration.
    # We exclude self. As of this revision, order here is only a TIE-BREAKER
    # of last resort (fallback when all candidates are affinity-blocked) —
    # the actual pick among conflict-free candidates is load-based (4-C).
    # -----------------------------------------------------------------------
    migration_candidates: >-
      {{
        groups['proxmox_nodes']
        | reject('eq', inventory_hostname)
        | list
      }}

    # Weights used for load-aware target selection, matching Play 6's metric
    # so migrations during reboot and the post-patch rebalance agree on what
    # "loaded" means. RAM is the priority metric; CPU is kept only as a
    # lightweight tiebreaker at 4x lower weight (per request — RAM balancing
    # is what actually matters here, CPU usage across nodes was fine).
    cpu_weight: 1
    ram_weight: 4   # per GB — 4x the weight of cpu_weight

    reboot_timeout: 600      # seconds to wait for node SSH after reboot
    cluster_settle_time: 60  # seconds to wait after node rejoins before proceeding

  tasks:
    # ------------------------------------------------------------------
    # Skip node if no new kernel was detected
    # ------------------------------------------------------------------
    - name: "Skip node if no kernel upgrade"
      ansible.builtin.meta: end_host
      when: not new_kernel_installed

    # ------------------------------------------------------------------
    # Step 4-A : Verify cluster quorum before touching this node
    # ------------------------------------------------------------------
    - name: "4-A | Check cluster quorum"
      ansible.builtin.command: pvecm status
      register: pvecm_status
      changed_when: false
      failed_when: "'Quorate' not in pvecm_status.stdout"

    - name: "4-A | Show quorum status"
      ansible.builtin.debug:
        msg: "{{ pvecm_status.stdout_lines }}"

    # ------------------------------------------------------------------
    # Step 4-B : Power off non-migratable VMs on this node
    # ------------------------------------------------------------------
    - name: "4-B | Power off non-migratable VMs"
      ansible.builtin.command: "qm shutdown {{ item }}"
      loop: "{{ non_migratable_vms[ansible_host] | default([]) }}"
      register: stop_result
      changed_when: true
      failed_when: stop_result.rc not in [0, 1]   # rc=1 = already stopped

    - name: "4-B | Wait for VMs to be fully stopped"
      ansible.builtin.command: "qm status {{ item }}"
      loop: "{{ non_migratable_vms[ansible_host] | default([]) }}"
      register: vm_status
      until: "'stopped' in vm_status.stdout"
      retries: 20
      delay: 10
      changed_when: false

    # ------------------------------------------------------------------
    # Step 4-C : Migrate all running VMs/CTs to another node,
    #            respecting HA anti-affinity rules AND balancing load.
    #
    # Anti-affinity strategy (read rules.cfg directly):
    #   Proxmox stores negative-affinity rules in /etc/pve/ha/rules.cfg.
    #   A rule block looks like:
    #
    #     affinity: my-rule-name
    #       resources vm:119,vm:120
    #       type negative
    #
    #   We parse this file with python to build a peer map: for each vmid,
    #   which other vmids must NOT share the same node.
    #
    # Target-selection strategy (CHANGED in this revision):
    #   Previously we picked the FIRST conflict-free candidate in inventory
    #   order, which meant every node's guests funneled onto the same
    #   "first" neighbor and then bounced back once that neighbor rebooted.
    #   Now, among all conflict-free candidates, we compute each one's
    #   CURRENT weighted load (vCPU*cpu_weight + RAM_GB*ram_weight, same
    #   metric Play 6 uses) via /cluster/resources and pick the LOWEST.
    #   This keeps guests spreading out across the reboot sequence instead
    #   of ping-ponging between two nodes.
    #
    #   Fallback: if every candidate is affinity-blocked, we still force
    #   onto the last candidate in the list with a loud WARNING — safety
    #   (getting off the rebooting node) always wins over balance.
    # ------------------------------------------------------------------
    - name: "4-C | Get list of running VMs on this node"
      ansible.builtin.shell: |
        qm list | awk 'NR>1 && $3=="running" {print $1}'
      register: running_vms
      changed_when: false

    - name: "4-C | Migrate running VMs (anti-affinity aware, load-balanced)"
      ansible.builtin.shell: |
        VMID="{{ item }}"
        CANDIDATES="{{ migration_candidates | join(' ') }}"
        RULES_CFG="/etc/pve/ha/rules.cfg"
        CPU_W="{{ cpu_weight }}"
        RAM_W="{{ ram_weight }}"

        # --- Step 1: find peers of VMID from rules.cfg ---
        PEERS=""
        if [ -f "$RULES_CFG" ]; then
          PEERS=$(python3 - "$VMID" <<'PYEOF'
        import sys, re

        target_vmid = sys.argv[1]
        target_sid  = f"vm:{target_vmid}"
        rules_cfg   = "/etc/pve/ha/rules.cfg"
        peers       = set()

        try:
            with open(rules_cfg) as f:
                content = f.read()
        except FileNotFoundError:
            sys.exit(0)

        blocks = re.split(r'\n(?=\S)', content.strip())
        for block in blocks:
            lines = block.strip().splitlines()
            if not lines or not lines[0].startswith("resource-affinity:"):
                continue
            resources = ""
            is_negative = False
            for line in lines[1:]:
                line = line.strip()
                if line.startswith("resources"):
                    resources = line.split(None, 1)[1] if len(line.split()) > 1 else ""
                if line.startswith("affinity") and "negative" in line:
                    is_negative = True
            if is_negative and target_sid in resources.split(","):
                for sid in resources.split(","):
                    sid = sid.strip()
                    if sid != target_sid and sid.startswith("vm:"):
                        peers.add(sid.split(":")[1])

        print(" ".join(peers))
        PYEOF
          )
        fi

        echo "VM ${VMID}: negative-affinity peers=[${PEERS}]"

        # --- Step 2: snapshot current cluster placements + running guests ---
        CLUSTER_VMS=$(pvesh get /cluster/resources --type vm --output-format json 2>/dev/null || echo "[]")

        # --- Step 3: build list of conflict-free candidates ---
        clean_candidates=""
        for node in $CANDIDATES; do
          conflict=0
          for peer in $PEERS; do
            peer_node=$(echo "$CLUSTER_VMS" | python3 -c "
        import sys,json
        data=json.load(sys.stdin)
        peer=sys.argv[1]
        for v in data:
            if str(v.get('vmid',''))==peer:
                print(v.get('node',''))
                break
        " "$peer" 2>/dev/null || true)
            if [ "$peer_node" = "$node" ]; then
              echo "  Conflict: peer vm:${peer} is on ${node}, excluding from candidates"
              conflict=1
              break
            fi
          done
          if [ "$conflict" -eq 0 ]; then
            clean_candidates="$clean_candidates $node"
          fi
        done
        clean_candidates=$(echo "$clean_candidates" | xargs)

        # --- Step 4: among conflict-free candidates, pick the LOWEST-LOADED ---
        chosen=""
        if [ -n "$clean_candidates" ]; then
          chosen=$(echo "$CLUSTER_VMS" | python3 -c "
        import sys, json
        data = json.load(sys.stdin)
        candidates = sys.argv[1].split()
        cpu_w = float(sys.argv[2])
        ram_w = float(sys.argv[3])

        scores = {n: 0.0 for n in candidates}
        for v in data:
            if v.get('status') != 'running':
                continue
            node = v.get('node')
            if node not in scores:
                continue
            cpus = v.get('maxcpu', 1)
            ram_gb = v.get('maxmem', 1073741824) / 1073741824
            scores[node] += cpus * cpu_w + ram_gb * ram_w

        best = min(scores, key=scores.get)
        print(best, file=sys.stderr)
        print(' '.join(f'{n}={scores[n]:.1f}' for n in candidates), file=sys.stderr)
        print(best)
        " "$clean_candidates" "$CPU_W" "$RAM_W" 2>/tmp/vm_${VMID}_scores.log)
          echo "  Candidate loads: $(cat /tmp/vm_${VMID}_scores.log | tail -1)"
          echo "  Selected: ${chosen} (lowest current weighted load)"
        fi

        # --- Step 5: fallback if every candidate was affinity-blocked ---
        if [ -z "$chosen" ]; then
          chosen=$(echo "$CANDIDATES" | awk '{print $NF}')
          echo "WARNING: all candidates blocked by anti-affinity for VM ${VMID}, forcing to ${chosen}" >&2
        fi

        echo "Migrating VM ${VMID} -> ${chosen}"
        qm migrate "$VMID" "$chosen" --online
      loop: "{{ running_vms.stdout_lines }}"
      when: running_vms.stdout_lines | length > 0
      register: migrate_vm_result

    - name: "4-C | Show VM migration results"
      ansible.builtin.debug:
        msg: "{{ item.stdout_lines | default([]) + item.stderr_lines | default([]) }}"
      loop: "{{ migrate_vm_result.results | default([]) }}"
      loop_control:
        label: "VM {{ item.item }}"
      when: migrate_vm_result.results is defined

    - name: "4-C | Get list of running LXC containers on this node"
      ansible.builtin.shell: |
        pct list | awk 'NR>1 && $2=="running" {print $1}'
      register: running_cts
      changed_when: false

    # LXC containers rarely carry anti-affinity rules, so we just pick the
    # least-loaded candidate directly (no conflict-filtering pass needed).
    - name: "4-C | Migrate running CTs to least-loaded candidate"
      ansible.builtin.shell: |
        CTID="{{ item }}"
        CANDIDATES="{{ migration_candidates | join(' ') }}"
        CPU_W="{{ cpu_weight }}"
        RAM_W="{{ ram_weight }}"

        CLUSTER_VMS=$(pvesh get /cluster/resources --type vm --output-format json 2>/dev/null || echo "[]")

        chosen=$(echo "$CLUSTER_VMS" | python3 -c "
        import sys, json
        data = json.load(sys.stdin)
        candidates = sys.argv[1].split()
        cpu_w = float(sys.argv[2])
        ram_w = float(sys.argv[3])

        scores = {n: 0.0 for n in candidates}
        for v in data:
            if v.get('status') != 'running':
                continue
            node = v.get('node')
            if node not in scores:
                continue
            cpus = v.get('maxcpu', 1)
            ram_gb = v.get('maxmem', 1073741824) / 1073741824
            scores[node] += cpus * cpu_w + ram_gb * ram_w

        print(min(scores, key=scores.get))
        " "$CANDIDATES" "$CPU_W" "$RAM_W")

        echo "Migrating CT ${CTID} -> ${chosen} (least-loaded candidate)"
        pct migrate "$CTID" "$chosen" --restart
      loop: "{{ running_cts.stdout_lines }}"
      when: running_cts.stdout_lines | length > 0
      register: migrate_ct_result

    - name: "4-C | Show CT migration results"
      ansible.builtin.debug:
        msg: "{{ item.stdout_lines | default([]) + item.stderr_lines | default([]) }}"
      loop: "{{ migrate_ct_result.results | default([]) }}"
      loop_control:
        label: "CT {{ item.item }}"
      when: migrate_ct_result.results is defined

    - name: "4-C | Pause to let migrations settle"
      ansible.builtin.pause:
        seconds: 30

    # ------------------------------------------------------------------
    # Step 4-D : Confirm node is now empty (no running guests)
    #
    # Both checks use a retry loop instead of a one-shot fail, because:
    #   - pct migrate --restart stops the CT on the source, starts it on the
    #     target — the source-side stop can lag behind the migrate command
    #     returning, especially for CTs with heavy memory or slow disks.
    #   - qm migrate --online for VMs can similarly take a moment to fully
    #     vacate the source node after the command exits.
    #
    # 15 retries x 20s = up to 5 minutes of patience per check.
    # If a guest is still present after that, we fail loudly with its ID
    # so you know exactly which guest blocked the reboot.
    # ------------------------------------------------------------------
    - name: "4-D | Wait until no VMs remain running on this node"
      ansible.builtin.shell: |
        qm list | awk 'NR>1 && $3=="running" {print $1}'
      register: remaining_vms
      changed_when: false
      failed_when: false
      retries: 15
      delay: 20
      until: remaining_vms.stdout_lines | length == 0

    - name: "4-D | Fail if VMs are still running after timeout"
      ansible.builtin.fail:
        msg: >-
          The following VMs are still running on {{ inventory_hostname }}
          after waiting for migration to complete: {{ remaining_vms.stdout_lines | join(', ') }}.
          Check migration status manually with: qm list
      when: remaining_vms.stdout_lines | length > 0

    - name: "4-D | Wait until no CTs remain running on this node"
      ansible.builtin.shell: |
        pct list | awk 'NR>1 && $2=="running" {print $1}'
      register: remaining_cts
      changed_when: false
      failed_when: false
      retries: 15
      delay: 20
      until: remaining_cts.stdout_lines | length == 0

    - name: "4-D | Fail if CTs are still running after timeout"
      ansible.builtin.fail:
        msg: >-
          The following CTs are still running on {{ inventory_hostname }}
          after waiting for migration to complete: {{ remaining_cts.stdout_lines | join(', ') }}.
          Check migration status manually with: pct list
      when: remaining_cts.stdout_lines | length > 0

    # ------------------------------------------------------------------
    # Step 4-E : Reboot the node
    # ------------------------------------------------------------------
    - name: "4-E | Reboot Proxmox node {{ inventory_hostname }}"
      ansible.builtin.reboot:
        reboot_timeout: "{{ reboot_timeout }}"
        pre_reboot_delay: 10
        post_reboot_delay: 30
        connect_timeout: 15
        msg: "Ansible rolling reboot — new PVE kernel detected"

    # ------------------------------------------------------------------
    # Step 4-F : Verify node rejoined the cluster and kernel updated
    # ------------------------------------------------------------------
    - name: "4-F | Get kernel version after reboot"
      ansible.builtin.command: uname -r
      register: kernel_after
      changed_when: false

    - name: "4-F | Confirm new kernel is running"
      ansible.builtin.debug:
        msg: >-
          {{ inventory_hostname }} rebooted successfully.
          Old kernel: {{ kernel_version_before }}
          New kernel:  {{ kernel_after.stdout }}

    - name: "4-F | Check cluster quorum after reboot"
      ansible.builtin.command: pvecm status
      register: pvecm_post
      changed_when: false
      # failed_when: false is critical — pvecm status returns rc=2 when the node
      # has not yet rejoined quorum. Without this, rc=2 causes a hard failure on
      # attempt 1, bypassing the retry loop entirely.
      failed_when: false
      # 20 retries x 20s = up to 400s (~6.5 min) for corosync to reform quorum.
      retries: 20
      delay: 20
      # pvecm outputs "Quorate:          Yes" with variable whitespace.
      # Use a regex search instead of a literal string match to be whitespace-safe.
      until: >-
        pvecm_post.rc == 0 and
        (pvecm_post.stdout | regex_search('Quorate:\s+Yes')) is not none

    - name: "4-F | Fail explicitly if quorum was never restored"
      ansible.builtin.fail:
        msg: >-
          Cluster quorum was NOT restored after {{ 20 * 20 }}s.
          Last pvecm output: {{ pvecm_post.stdout }}
          Manual intervention required before proceeding to the next node.
      when: >-
        pvecm_post.rc != 0 or
        (pvecm_post.stdout | regex_search('Quorate:\s+Yes')) is none

    - name: "4-F | Show quorum restored"
      ansible.builtin.debug:
        msg: "Quorum restored on {{ inventory_hostname }} — proceeding to settle wait."

    - name: "4-F | Wait {{ cluster_settle_time }}s for cluster to fully stabilise"
      ansible.builtin.pause:
        seconds: "{{ cluster_settle_time }}"

    # ------------------------------------------------------------------
    # Step 4-G : Restore non-migratable VMs
    # ------------------------------------------------------------------
    - name: "4-G | Start non-migratable VMs"
      ansible.builtin.command: "qm start {{ item }}"
      loop: "{{ non_migratable_vms[ansible_host] | default([]) }}"
      register: start_result
      changed_when: true

    - name: "4-G | Wait for VMs to reach running state"
      ansible.builtin.command: "qm status {{ item }}"
      loop: "{{ non_migratable_vms[ansible_host] | default([]) }}"
      register: vm_start_status
      until: "'running' in vm_start_status.stdout"
      retries: 20
      delay: 10
      changed_when: false

    - name: "4-G | Confirm all non-migratable VMs are back online"
      ansible.builtin.debug:
        msg: >-
          Node {{ inventory_hostname }} — restored VMs:
          {{ non_migratable_vms[ansible_host] | default([]) | join(', ') }}


##############################################################################
# PLAY 5 – Final cluster health report
##############################################################################
- name: "PLAY 5 | Final cluster health check"
  hosts: proxmox_nodes[0]   # run once from the first node
  gather_facts: false
  become: true

  tasks:
    - name: "5 | Cluster node list"
      ansible.builtin.command: pvecm nodes
      register: pvecm_nodes
      changed_when: false

    - name: "5 | Cluster status"
      ansible.builtin.command: pvecm status
      register: pvecm_final
      changed_when: false

    - name: "5 | Display final cluster report"
      ansible.builtin.debug:
        msg:
          - "===== FINAL CLUSTER HEALTH ====="
          - "{{ pvecm_nodes.stdout_lines }}"
          - "================================"
          - "{{ pvecm_final.stdout_lines }}"

    - name: "5 | List all running guests across the cluster"
      ansible.builtin.shell: |
        pvesh get /cluster/resources --type vm 2>/dev/null \
          | grep '"status":"running"' | grep -oP '"name":"\K[^"]+' || true
      register: running_guests
      changed_when: false

    - name: "5 | Running guests report"
      ansible.builtin.debug:
        msg: "Running guests: {{ running_guests.stdout_lines }}"


##############################################################################
# PLAY 6 – Cluster load rebalancing (CPU + RAM weighted, VMs AND LXC CTs)
#
# Runs automatically after patching to spread guests evenly across all 3
# nodes. Covers both QEMU VMs and LXC containers — see CHANGELOG above.
#
# Scope:
#   - Only considers RUNNING VMs and CTs
#   - Excludes guests pinned by node-affinity strict rules (rules.cfg,
#     "vm:" and "ct:" resource-ids)
#   - Excludes guests in the non_migratable_vms map (aliased from Play 4 —
#     see pinned_vmids below; this is the single source of truth, not a copy)
#   - Respects resource-affinity negative rules when choosing destination
#
# Load metric: weighted score = (vCPUs * cpu_weight) + (RAM_GB * ram_weight)
#   RAM is weighted 4x higher than CPU — RAM balancing is the priority here.
# Balance threshold: stops when max_node_score - min_node_score < threshold
#   fraction of total. TIGHTENED in this revision (0.20 -> 0.05) so the
#   cluster converges much closer to flat instead of stopping at "good enough".
# Move cap: RAISED in this revision (20 -> 50) so a badly-skewed cluster
#   (e.g. right after a rolling reboot) has enough iterations to fully settle.
#
# All logic runs as a single Python3 script on the first cluster node,
# emitting an ordered list of (vmid, source_node, dest_node) moves.
# Ansible then executes the moves sequentially with a settle pause.
##############################################################################
- name: "PLAY 6 | Rebalance VM load across cluster nodes"
  hosts: proxmox_nodes[0]
  gather_facts: false
  become: true

  vars:
    # Weights for the load score. RAM is the priority metric; CPU is kept
    # only as a lightweight tiebreaker at 4x lower weight (usage was already
    # fine across nodes — RAM is what needs balancing).
    cpu_weight: 1
    ram_weight: 4   # per GB — 4x the weight of cpu_weight

    # Max spread tolerated before we stop rebalancing:
    # if (max_score - min_score) / total_score < this, cluster is balanced.
    # Tightened from 0.20 -> 0.05 for a more aggressive rebalance.
    balance_threshold: 0.05

    # Seconds to wait between individual VM migrations
    migrate_settle_seconds: 20

    # Same anchored map Play 4 uses for shutdown/restart-in-place — aliased
    # here rather than duplicated, so there is exactly one place that says
    # "this VM is pinned". pinned_vmids is derived from it automatically,
    # so a VM added to the map is excluded from rebalancing everywhere
    # without needing a second edit. (Previously this was a hand-maintained
    # list that had drifted out of sync with Play 4's map — VM 119, 120 and
    # 126 were missing from it, so Play 6 was free to move guests that were
    # actually meant to stay put, e.g. VM 119 pinned to PVE1-dell.)
    non_migratable_vms: *non_migratable_vms_map
    pinned_vmids: "{{ non_migratable_vms.values() | list | flatten }}"

  tasks:
    - name: "6 | Write rebalance script to disk"
      ansible.builtin.copy:
        dest: /tmp/pve_rebalance.py
        mode: "0700"
        content: |
          import subprocess, json, re, sys

          CPU_W     = float(sys.argv[1])
          RAM_W     = float(sys.argv[2])
          THRESHOLD = float(sys.argv[3])
          PINNED    = set(sys.argv[4].split(",")) if sys.argv[4] else set()
          RULES_CFG = "/etc/pve/ha/rules.cfg"

          def pvesh(path):
              raw = subprocess.check_output(
                  ["pvesh", "get", path, "--type", "vm", "--output-format", "json"],
                  stderr=subprocess.DEVNULL, text=True)
              return json.loads(raw)

          try:
              rules_text = open(RULES_CFG).read()
          except FileNotFoundError:
              rules_text = ""

          blocks = re.split(r'\n(?=\S)', rules_text.strip())
          node_pinned = {}
          neg_peers   = {}

          for block in blocks:
              lines = [l.strip() for l in block.strip().splitlines()]
              if not lines:
                  continue
              header = lines[0]
              props = {}
              for l in lines[1:]:
                  parts = l.split(None, 1)
                  if len(parts) == 2:
                      props[parts[0]] = parts[1]

              if header.startswith("node-affinity:") and props.get("strict", "0") == "1":
                  for sid in props.get("resources", "").split(","):
                      sid = sid.strip()
                      # "vm:" = QEMU VM, "ct:" = LXC container — both are
                      # valid HA resource-id prefixes in rules.cfg.
                      if sid.startswith("vm:") or sid.startswith("ct:"):
                          vmid = sid.split(":", 1)[1]
                          allowed = set(n.strip() for n in props.get("nodes", "").split(","))
                          node_pinned[vmid] = allowed

              if header.startswith("resource-affinity:") and props.get("affinity") == "negative":
                  members    = [s.strip() for s in props.get("resources", "").split(",")]
                  member_ids = [s.split(":", 1)[1] for s in members
                                if s.startswith("vm:") or s.startswith("ct:")]
                  for vmid in member_ids:
                      if vmid not in neg_peers:
                          neg_peers[vmid] = set()
                      neg_peers[vmid].update(p for p in member_ids if p != vmid)

          cluster_vms = pvesh("/cluster/resources")
          nodes = sorted(set(v["node"] for v in cluster_vms if "node" in v))

          # "vm" (--type vm) returns BOTH qemu VMs and lxc containers, tagged
          # via the per-entry "type" field ("qemu" or "lxc"). Previously this
          # loop only kept type=="qemu", silently dropping every LXC
          # container from load scoring and rebalancing — CTs were never
          # touched. Both types are now included and tracked so the move
          # executor below can issue the right migrate command per guest.
          vm_info = {}
          for v in cluster_vms:
              vtype = v.get("type")
              if vtype not in ("qemu", "lxc") or v.get("status") != "running":
                  continue
              vmid = str(v["vmid"])
              if vmid in PINNED or vmid in node_pinned:
                  continue
              vm_info[vmid] = {
                  "node":   v["node"],
                  "type":   vtype,
                  "cpus":   v.get("maxcpu", 1),
                  "ram_gb": v.get("maxmem", 1073741824) / 1073741824,
              }

          def score(vm):
              return vm["cpus"] * CPU_W + vm["ram_gb"] * RAM_W

          moves       = []
          moved_vmids = set()   # each VM moves at most once per run — prevents
                                 # the same guest flip-flopping back and forth
                                 # when there isn't enough granularity among
                                 # running VMs to hit THRESHOLD exactly.
          MAX_MOVES = 50   # was 20 — more room to fully flatten a skewed cluster

          for _ in range(MAX_MOVES):
              node_score = {n: 0.0 for n in nodes}
              node_vms   = {n: [] for n in nodes}
              for vmid, info in vm_info.items():
                  s = score(info)
                  node_score[info["node"]] += s
                  node_vms[info["node"]].append((vmid, s))

              total = sum(node_score.values())
              if total == 0:
                  break

              max_node = max(node_score, key=node_score.get)
              min_node = min(node_score, key=node_score.get)
              spread   = (node_score[max_node] - node_score[min_node]) / total

              if spread < THRESHOLD:
                  break

              candidates = sorted(node_vms[max_node], key=lambda x: x[1], reverse=True)
              moved = False
              for vmid, _ in candidates:
                  if vmid in moved_vmids:
                      continue
                  peers        = neg_peers.get(vmid, set())
                  peers_on_min = [p for p in peers if vm_info.get(p, {}).get("node") == min_node]
                  if peers_on_min:
                      continue
                  moves.append({"vmid": vmid, "from": max_node, "to": min_node,
                                "type": vm_info[vmid]["type"]})
                  vm_info[vmid]["node"] = min_node
                  moved_vmids.add(vmid)
                  moved = True
                  break

              if not moved:
                  # Either every VM on the most-loaded node is pinned by
                  # anti-affinity, or every movable VM here has already been
                  # moved once this run. Either way, moving further would mean
                  # re-moving an already-moved guest (oscillation) rather than
                  # making real progress, so stop here even if spread is still
                  # above THRESHOLD.
                  break

          print(json.dumps(moves))

    - name: "6 | Compute rebalance move plan"
      ansible.builtin.command: >
        python3 /tmp/pve_rebalance.py
        {{ cpu_weight }}
        {{ ram_weight }}
        {{ balance_threshold }}
        {{ pinned_vmids | join(',') }}
      register: rebalance_plan_raw
      changed_when: false

    - name: "6 | Parse move plan"
      ansible.builtin.set_fact:
        rebalance_moves: "{{ rebalance_plan_raw.stdout | from_json }}"

    - name: "6 | Show rebalance plan"
      ansible.builtin.debug:
        msg: >-
          {% if rebalance_moves | length == 0 %}
          Cluster is already balanced — no moves needed.
          {% else %}
          Planned {{ rebalance_moves | length }} move(s):
          {% for m in rebalance_moves %}
            {{ 'VM' if m.type == 'qemu' else 'CT' }} {{ m.vmid }}: {{ m.from }} → {{ m.to }}
          {% endfor %}
          {% endif %}

    # qm migrate / pct migrate are LOCAL commands — they must run on the node
    # that currently owns the guest (item.from), never from a fixed
    # "coordinator" node. Without delegate_to here, every migration was
    # issued from proxmox_nodes[0] regardless of where the guest actually
    # lives, which fails immediately ("target is local node" when
    # from==self, "config does not exist" when the guest isn't local to the
    # executing node).
    #
    # Command differs by guest type: qm migrate supports --online (true live
    # migration) for QEMU VMs; pct migrate uses --restart for LXC containers
    # (stop on source, start on target — matches the CT migration approach
    # already used in Play 4's step 4-C).
    - name: "6 | Execute VM/CT migrations"
      ansible.builtin.command: >
        {{ 'qm migrate' if item.type == 'qemu' else 'pct migrate' }}
        {{ item.vmid }} {{ item.to }}
        {{ '--online' if item.type == 'qemu' else '--restart' }}
      loop: "{{ rebalance_moves }}"
      loop_control:
        label: "{{ 'VM' if item.type == 'qemu' else 'CT' }} {{ item.vmid }}: {{ item.from }} → {{ item.to }}"
      when: rebalance_moves | length > 0
      delegate_to: "{{ item.from }}"
      register: rebalance_migrate_result

    - name: "6 | Pause between migrations to let cluster settle"
      ansible.builtin.pause:
        seconds: "{{ migrate_settle_seconds }}"
      loop: "{{ rebalance_moves }}"
      loop_control:
        label: "Settling after {{ 'VM' if item.type == 'qemu' else 'CT' }} {{ item.vmid }}"
      when: rebalance_moves | length > 0

    - name: "6 | Rebalance complete — final node load summary"
      ansible.builtin.shell: |
        python3 - <<'PYEOF'
        import subprocess, json
        cluster_vms = json.loads(subprocess.check_output(
            ["pvesh", "get", "/cluster/resources", "--type", "vm", "--output-format", "json"],
            stderr=subprocess.DEVNULL, text=True))
        cpu_w, ram_w = {{ cpu_weight }}, {{ ram_weight }}
        node_score = {}
        node_count = {}
        for v in cluster_vms:
            if v.get("type") not in ("qemu", "lxc") or v.get("status") != "running":
                continue
            n = v["node"]
            node_score[n] = node_score.get(n, 0) + v.get("maxcpu", 1) * cpu_w + (v.get("maxmem", 1073741824) / 1073741824) * ram_w
            node_count[n] = node_count.get(n, 0) + 1
        for n in sorted(node_score):
            print(f"  {n}: {node_count[n]} guests (VMs+CTs), weighted score={node_score[n]:.1f}")
        PYEOF
      register: final_summary
      changed_when: false

    - name: "6 | Show final load summary"
      ansible.builtin.debug:
        msg: "{{ final_summary.stdout_lines }}"

##############################################################################
# PLAY 7 – Re-apply PVE sensors mod on all nodes after patching
##############################################################################
- name: "PLAY 7 | Re-apply PVE sensors GUI mod on all nodes"
  hosts: proxmox_nodes
  gather_facts: false
  become: true
  serial: 1

  tasks:
    - name: "7 | Uninstall existing sensors mod (ignore if not installed)"
      ansible.builtin.shell: |
        printf 'y\n' | timeout 30 bash /root/pve-mod-gui-sensors.sh uninstall
      register: sensors_uninstall
      failed_when: false
      changed_when: "'Uninstalling' in sensors_uninstall.stdout"

    - name: "7 | Show uninstall output"
      ansible.builtin.debug:
        var: sensors_uninstall.stdout_lines

    - name: "7 | Re-install sensors mod"
      ansible.builtin.shell: |
        printf 'Y\nY\nY\nc\nN\n' | timeout 60 bash /root/pve-mod-gui-sensors.sh install
      register: sensors_install
      failed_when: >
        sensors_install.rc != 0 and
        'already installed' not in sensors_install.stdout

    - name: "7 | Show install output"
      ansible.builtin.debug:
        var: sensors_install.stdout_lines
